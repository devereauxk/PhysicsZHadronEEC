analyze the project PhysicsZHadronEEC as it is for the ability to run agent  simulatneously. For instance I am running a claude cli right now which is executing many ExecuteCorrelationsAnalysis in parallel as well as a copilot cli instance doing the same. I want 
  to make sure that I can do this safely without recompiling exectables that are in use. For instance if one agents is half way 
  through running ExecuteCorrelationAnalysis and the second recompiles it, we could end up with production roots corrupted with data 
  from two different working points, or seg faults. Report back what is currently done for ExecuteCorrelationAnalysis executables, 
  CommonCode executables, as well as any others similar files we made want to be careful about. What is done right now? Are these 
  things automatically recompiled when a job is started or only when analysis code changes. Make suggestiosn for best practices and 
  modifications and put them in a .md file at .claude/.
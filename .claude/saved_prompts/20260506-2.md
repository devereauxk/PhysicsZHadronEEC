# introduction
Read the guidelines at .github/copilot-instructions. You have the role of reviewer, you will write plans for analyzer and then start an analyzer subagent to carry out that those plans. It is up to you how to schedule and send off the tasks to analyzer subagent(s), but keep the work of each subagent isolated. Only use GPT-5.4 for subagent models. Be careful of recompiling executables which might be in use by another program. If there is a chance a file is being edited or executable ran or will soon-to-be ran by anther subagent, then wait until that subagent's task has concluded. For analysis production use 20-30 threads, for skimming use 5 threads for mc and 10 threads for data.

# 12x12-bin results for official modified binning
We are modifying the rebinned result following issues with the recent production.

Propagate the following changes to the official data analysis workflow. Hard code the 12-bin scheme for the deltaeta (now 12 bins) and deltaphi (now 12 bins) dimensions. Do not auto-detect anything set all bins specifically in both the main analysis and makeProjection. Remove the getProjectedWindow framework and all the crap used to auto calculate bin edges.

Hard code the deltaeta region to be from -4 to 4 with 12 bins, and hard code -pi/2 to 3pi/2 for the deltaphi region using 12 bins. Propagate the hard coded binning through the projection file, and confirm we removed the structure that allows for various binnings.

For the 1D projections integrate over deltaeta bins corresponding to 0 to 4 in deltaeta (the positive region) and deltaphi bins corresponding with 0 to pi/2. Ensure that there are indeed bin edges at these values in both dimensions, and return if that is not the case. Make sure this projection makes it such that the double counting done in the analysis is handled downstream correctly, and when we scale by 0.5 during plotting. Propagate this to the results plots as well. Results should be plotted with the same axes scales as deltaeta and deltaphi are filled.

Calculate and graph the systematics on the inclusice and scan result 10-bin plots for the kinematic regions
 - 0-500 Zpt, 0.5-15 trk pt
 - 0-30 30-500 Zpt, 0.5-2 2-4 4-15 trk pt
Include pp and pPb result curves as well as powheg curve. Use a different systematics runner script than the one for the 20-bin result systematics. Calculate ALL systematics for these 12-bin results, the systematics weights used in the official weight dictionary are still valid and should be used. Reference the official weight dictionary, systematics section of the analysis note, and 20-bin systemaics .sh runner for the structure. Analyze and produce plots for the inclusice range first before moving on to the scan results. Update me mid-run pointing to these plots so I can confirm these look correct.

Plot the systematics error bars in the same style as the result plots in the overleaf right now. Make a new presentation at central_combined_bin12x12_20260507.pdf with the new systematics - just add the inclusive and scan productions made above, no need to add all the others that the 10x10 presentation has. Add new figures after the 20-bin figures in the Results section of the analysis note with the analogue 12-bin results (replace the 10x10 plots with the 12x12 oners). Label the figures in the same way and make a minimal edit to the text to cite the figures. Furthermore, make a 2x3 single-pdf graphic in Plots/20260213_Central/overleaf_pdf_composites for the scan deltaeta and deltaphi results for 12x12 results.


# conclusion
Make the minimum number of edits necessary to implement functionality. Do not add superflous error statement catches or checks. 

Have the subanalyzer handoff a summary to you and you should review that summary for flaws. Update copilot instructions on the new norms detailed here and learned by you or the subanalyzer throught these tasks.

# introduction
Read the guidelines at `CLAUDE.md`. You have the role of reviewer, you will write plans and then carry out that those plans. It is up to you how to schedule and send off the tasks to analyzer subagent(s) or complete them yourself, but keep the work of agents isolated. Be careful of recompiling executables which might be in use by another program. If there is a chance a file is being edited or executable ran or will soon-to-be ran by anther subagent, then wait until that subagent's task has concluded. For analysis production use 20-30 threads, for skimming use 5 threads for mc and 10 threads for data.


# appendix: pion mass study rerun
Remake the plots in the pion mass study using the new residual weights and copy them to the appendix section.

# appendix: Nmix study
Through the study we use Nmix=10 whenever background subtraction is performed. We do this since annectodally increasing this much higher did not seem to reduce the statisitcal fluctations of the results meaningfully. We will show this quantitatively.

As writer, make a new section in the analysis note appendix called "Setting $N_\text{Mix} for background subtraction". In this section create the following plots:
 - inclusive selection deltaeta, deltaphi results showing pp (energy extrapolated), pPb (combined), Powheg+EPOS (gen) - the same curves as the result plots - but split into different plots where we overlay curves calculated with different nmixes. Try out nmix=1,5,10,15,20. So for example one plot will be pp data deltaphi, which should have 5 curves. (use the analyzer role to generate these). Show the difference between nmix=? - nmix=20 as a lower difference plot.
 - the same plots but the mixed-event correlation function ("bkg") instead of the result.
No need to recalculate any corrections for this, just use the nmix=10-derived ones in the dictionary - we do not need to be that careful here. For these plots be sure you graph the jackknife uncertainty for each nmix. Here is it important to do so as increasing nmix should reflect as a reduction in stat. uncertainty for these curves.
Give a description of the study as a paragraph and \ref the figures. Organize the pdfs appropriately between figures to be in line with the rest of the figures in the note. Maybe doing two-figure panels would be good, showing "bkg" in the left and result on the right, one 3x2 figure for 3 collision species for deltaphi, and another 3x2 figure for deltaeta.


# conclusion
Make the minimum number of edits necessary to implement functionality. Do not add superflous error statement catches or checks. 

Read and update `CLAUDE.md` on the new norms detailed here and learned by you or subanalyzers throught these tasks.

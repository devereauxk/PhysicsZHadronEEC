# introduction
Read the guidelines at `CLAUDE.md`. You have the role of reviewer, you will write plans and then carry out that those plans. It is up to you how to schedule and send off the tasks to analyzer subagent(s) or complete them yourself, but keep the work of agents isolated. Be careful of recompiling executables which might be in use by another program. If there is a chance a file is being edited or executable ran or will soon-to-be ran by anther subagent, then wait until that subagent's task has concluded. For analysis production use 20-30 threads, for skimming use 5 threads for mc and 10 threads for data.

# writer role
Study the analysis note overleaf and write a new "writer" role in CLAUDE.md to be used to direct agents whose task is to edit the overleaf text, modifying/adding sections, they will not write code. Note the style choices of the note, for example there are no references to actual code, syntax, variables names, or file names. The writer should write a high-level with knowledge of the physics but agnostic to this project's implementation. Note conventions such as common phrases to use, section organization, acronyms. The writer should be constrained to the limited number of tasks given to it, it should not edit or even check typos except for the section(s) for which it is dispatched. Note any other important style points that should be propagated. The goal is the writer should produce text that exactly blends in with the human-authored sections of the note.

## appendix: pion track study
As a test of the writer role, make a new appendix in the analysis note "Testing $y^\text{ch} \sim \etat$ with pion--mass track assumption" to put the results of the pion track study. Make a 2x2 latex figure there of the delta phi (right) and delta eta (left) central values, pp in first row, then pPbPbp in second row. Breifly describe and \ref the plot in a paragraph in this appendix section. Mention that the assumption we make throughout the note that $y^\text{ch} \sim \etat$ is tested assuming tracks have mass of a pion (give pion mass). This is checked against the nominal result and negligable difference is seen, so it is a safe assumption. Complete this section before moving to the 2D promotion task.


# promotion of 2D residual correction to official weights
The 2D residual correction is to be promoted as the official residual correction framework. Implement the following changes.

## official weight migration to 2D
Make a copy of the OfficialWeightDictionary.sh and call it OfficialWeightDictionary_ANV3V4.sh to save the previous frozen weights.

Upgrade the current trk version tag to `trkV28`. Regenerate the new 2D corrections such that they have this tag in their file name. Ensure today's date is reflected in the correction root file names as well. Continue to use the same Z version tag number.

Use residual weights R_WEIGHT_* files in OfficialWeightDictionary.sh as a laundry list of residual weights to recalculate. We have already remade the central value residual weights but many systematics require new ones to be reran as well. Rerun those weights and replace the R_WEIGHT variables in the dictionary to point to the new weight files. Additionally, the energy extrapolation weights should all be remade as well using the new residual track weights since the EE weights are calculated on top of those track weights. Besides the residual track and energy extrapolation weights, all other weights should continue to use the ANV3V4 working point.

Compute the production .roots and the MainAnalysis CorrelationAnalysis.cpp file. Make application of the 2D residual the norm in the main analysis script. The 1D method is now deprecated. Make productions for the kinematic selections
 - inclusive: Zpt 0-500, trk pT 0.5-15
 - scan: ZpT: 0-30, 30-500, trk pT 0.5-2 2-4 4-15

## plot deliverables
Make new closure plots with the 2D residual correction for the track residual correction section in the note. Use the same kinematic selections there. There are some plots there that also get set to the appendix so update those too. Use the same plotting script at 20251202_trackResidualCorrection. Replace the closure plots in the overleaf.

Also replace the iterative correction plots in that section with the new 3x2 closure plots with the pt, eta-phi heat map, i.e. 

Make minimal edits to the track residual correction text, using standard of the writer role, to update the correction description to mention 2D eta-phi correction.

Remake all systematics section plots with productions using the new track residuals calculated on nominal result and systematic variation track residuals. Use the note as reference. Propagate the same scheme for systematic and statistical uncertainty calculations. Ensure the result plots use jackknife resampling. Remake the pPb and Pbp combination plots, and rerun the statistical agreement test, updating the pvalue table with new values.

Remake the result plots. In both the 20-bin and 12-bin structure - just as is done now. Remaking all productions may take significant time.


# conclusion
Make the minimum number of edits necessary to implement functionality. Do not add superflous error statement catches or checks. 

Read and update `CLAUDE.md` on the new norms detailed here and learned by you or subanalyzers throught these tasks.

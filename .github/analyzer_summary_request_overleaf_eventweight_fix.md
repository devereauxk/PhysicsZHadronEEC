# Request to Analyzer: Post-Reprocessing Summary Needed

Please execute `.github/overleaf_impact_reprocess_plan_after_eventweight_fix.md` and submit a completion summary for reviewer approval.

Your summary must include:
- Command log (ordered, copy-paste exact commands).
- GEN MC normalization table for pPb/PbP (`DeltaPhi` integrals by Z bin, threshold used).
- List of all regenerated closure PDFs and all files copied to Overleaf.
- Confirmation that Overleaf closure figure references in `src/analysis.tex` and `src/appendix.tex` are now backed by regenerated files.
- Explicit statement on pp plot impact: changed or unchanged (with reason).

Do not omit failed attempts; include them with resolution steps.

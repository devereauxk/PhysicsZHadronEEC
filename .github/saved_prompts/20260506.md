# introduction
Read the guidelines at .github/copilot-instructions. You have the role of reviewer, you will write plans for analyzer and then start an analyzer subagent to carry out that those plans. It is up to you how to schedule and send off the tasks to analyzer subagent(s), but keep the work of each subagent isolated. Be careful of recompiling executables which might be in use by another program. If there is a chance a file is being edited or executable ran or will soon-to-be ran by anther subagent, then wait until that subagent's task has concluded. For analysis production use 20-30 threads, for skimming use 5 threads for mc and 10 threads for data.

# projection window bug fix
Repair the issue discovered in the last prompt that the FindFixBin functions correctly computes the sideband bounds for the deltaeta and deltaphi projections. Verify the fix by making sure the 20-bin case which is used in the frozen note where the deltaphi histogram selects on deltaeta bins 0 through 10 incluisve - corresponding with -4 < deltaeta < 0 and deltaeta selects on deltaphi bins 0 through 10 inclusive - corresponding with 0 < deltaphi < pi/2. Implement the fix directly in the main analysis pipeline. Fix any other effects this projection bug may have had on the analysis pipeline.

Using the bins where the 5% residual integral mismatch was found, recalculate them and their integrals. Ensure the mismatch now disappears with the fixed projection script. Return back with metric on how the discrepancy was fixed. If they do not close signifcantly better then iterate until the bug is solve and explain.

Regenerate the official 20-bin results in the analysis note right now. Use the full datasets for this, replace the official production histograms, do it for the inclusive and scan results, and copy the new plots to the overleaf.

Just to be safe regenerate the pPb and Pbp comparison plots corresponding with the pPb-vs-Pbp overlay figures in the analysis section. You will also see that this section now includes a table with the chi^2 and p-value results of the stat error calculation methods. Recalculate these numbers. Migrate the scripts used to do this to a new directory at MainAnalysis/20260506_Jackknife and run them from there. In theory these p-values shouldn't change as the FindFixBin bug was after these plots were made. Report anyways if they change and update them directly in the note if they do.

I assume the bug also propagates to every plot in Plots/20260213_Central/presentations/central_combined_bin10x10_20260502.pdf/tex. Regenerate all of these plots in all kinematic regions studied. Replace the current pdf with the refreshed one.

Ensure also that the jackkinfe technique is the STANDARD now for plotting
 - all result plots
 - pPb-vs-Pbp comparison
And that this standard is in the copilot-instructions and all future versions of these plots should be made with these errors.


# conclusion
Make the minimum number of edits necessary to implement functionality. Do not add superflous error statement catches or checks. 

Have the subanalyzer handoff a summary to you and you should review that summary for flaws. Update copilot instructions on the new norms detailed here and learned by you or the subanalyzer throught these tasks.

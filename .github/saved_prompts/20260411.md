# introduction
Read the guidelines at .github/copilot-instructions. You have the role of reviewer, you will write plans for analyzer and then start an analyzer subagent to carry out that those plans. It is up to you how to schedule and send off the tasks to analyzer subagent(s), but keep the work of each subagent isolated. Be careful of recompiling executables which might be in use by another program. If there is a chance a file is being edited or executable ran or will soon-to-be ran by anther subagent, then wait until that subagent's task has concluded.


# follow up on 20260410.md instructions
Fix the following plots and copy the replacements to the overleaf.
 - Investigate the pp weighting scheme for the pp systematics and results plots. We see the nominal pp case in the systematics undershoot the other pp variations subtantailly. Consider the energy extrapolation weights applied (make sure the are applied to the nominal case as well as the variations). Confirm also that the current energy weight is calculated through ratios of data Z pt spectra i.e. pPb data 8.16 / pp data 5.02, and NOT the MC-driven correction using the pythia+madgraph. That generation is simply for the systematic variation. If this does not answer the descrepancy then confirm all other official weights are applied properly. Once the weight is fixed, regenerate all affected pp systematics plots. The same issue seems to have propagated to the result plots too so regenerate the result pp curves and plots also.
 - Fig. 2 [still an issue] The pp/pPb/Pbp HLT trigger turn on plot is missing in the overleaf, or else is not being outputted correctly/has buggy implementation. Remake the plot and ensure all curves are present before copying over to the overleaf.
 - Fig. 12, map of track-muon delta eta and delta phi in track reconstruction section. These plots are wrong - these should be made with the current V0.3 skims, and use isMuTagged=false, so we can see all the muon candidiates whioch we cut inside 0.0025. Overlay circles with radii 0.0025 and 0.004.


# conclusion
Make the minimum number of edits necessary to implement functionality. Do not add superflous error statement catches or checks. 

Have the subanalyzer handoff a summary to you and you should review that summary for flaws. Update copilot instructions on the new norms detailed here and learned by you or the subanalyzer throught these tasks.

